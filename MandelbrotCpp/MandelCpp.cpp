#include "pch.h"
#include "MandelCpp.h"

int generateMandelCpp(BYTE* bmp, int rowCount, int rowNum, int resX, int resY, int iterCount)
{
	int test = resX + resY + iterCount + iterCount + int(bmp);
	return test;
}